\documentclass[tikz,convert={outfile=\jobname.svg}]{standalone}
\usepackage{tikz}
\usepackage{pagecolor}
\usetikzlibrary{arrows,shapes.geometric,arrows.meta,calc,chains,scopes,shapes.misc,backgrounds}
\pgfdeclarelayer{background}
\pgfdeclarelayer{foreground}
\pgfsetlayers{background,main,foreground}
\pagecolor{white}

\tikzset{ 
   line/.style  = {-, line width=0.5mm, },
   arrow/.style = {-latex,line width=0.5mm, },  
}

\begin{document}
   \begin{tikzpicture} 
     
      \node[
         rectangle,
         draw,
         font=\ttfamily,
         line width = 0.5mm,
         minimum height=20mm,
         minimum width=20mm,
      ] (ram) at (-70mm,30mm){RAM};

     \node[
         rectangle,
         draw,
         font=\ttfamily,
         line width = 0.5mm,
         minimum height=10mm,
         minimum width=40mm,
      ] (inst) at (-30mm,10mm){Instructions};

      \node[
         rectangle,
         draw,
         font=\ttfamily,
         line width = 0.5mm,
         minimum height=10mm,
         minimum width=40mm,
      ] (data) at (10mm,10mm){Data};
 
      \node [
         trapezium,   
         draw,   
         shape border rotate = 180,  
         line width = 0.5mm,
         inner xsep=10mm,
         inner ysep=2mm,
      ] (mux) at (-30mm,-15mm) {};
        
      \node[
         rectangle,
         draw,
         font=\ttfamily,
         line width = 0.5mm,
         minimum height=20mm,
         minimum width=20mm,
      ] (cnt) at (-30mm,-35mm){Count};

      \node[
         circle,
         draw,
         font=\ttfamily,
         line width = 0.5mm,
         minimum height=15mm,
         minimum width=15mm,
      ] (subber) at (-70mm,-35mm){-1};



      \draw[line] (subber.north) |- (-35mm,-5mm)  |- ([yshift=0mm,xshift=-5mm]mux.north);
      \draw[line] (inst.south)  |- (mux.north);
      \draw[line] (cnt.south)   |- (-70mm,-50mm) |-(subber.south);
      \draw[line] (data.south)  |- (-25mm,-5mm)  |-([yshift=0mm,xshift=5mm]mux.north);
      \draw[line] (mux.south)   |- (cnt.north);  
      \draw[line] (subber.north) |- (ram.south);
      \draw[line] (ram.east)    |- (-30mm,30mm)  |- (inst.north);
      \draw[line] (ram.east)    |- (10mm,30mm)   |- (data.north);

   \end{tikzpicture}
\end{document}
